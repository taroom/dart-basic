void main() {
  // null is a special data type in Dart that represents the absence of a value.
  // String fullname; // fullname harus diassign / diisi
  // print(fullname); // This will cause an error because fullname is not initialized.
  String? name; // name can be null
  int? age; // age can be null
  double? height; // height can be null
  bool? isEmployed; // isEmployed can be null

  print(name);
  print(age);
  print(height);
  print(isEmployed);
}
